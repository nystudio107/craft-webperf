{% if headless is not defined or not headless %}
    var webperf_config = {
        url: '{{ boomerangScriptUrl | escape("js") }}',
        title: '{{ boomerangTitle }} | escape("js")' || document.title || '',
        requestId: '{{ boomerangRequestId }} | escape("js")',
    };
{% endif %}
