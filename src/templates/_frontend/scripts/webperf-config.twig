{% if headless is not defined or not headless %}
    var webperf_config = function() {
        this.url = '{{ boomerangScriptUrl }}';
        this.title = '{{ boomerangTitle }}' || document.title;
        this.requestId = '{{ boomerangRequestId }}';
    };
{% endif %}
