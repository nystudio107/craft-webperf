{% if headless is not defined or not headless %}
    var webperf_config = {
        url: '{{ boomerangScriptUrl }}',
        title: '{{ boomerangTitle }}' || document.title || '',
        requestId: '{{ boomerangRequestId }}',
    };
{% endif %}
