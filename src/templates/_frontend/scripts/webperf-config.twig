{% if headless is not defined or not headless %}
    var webperf_config = {
        url: '{{ boomerangScriptUrl | raw }}',
        title: '{{ boomerangTitle | raw }}' || document.title || '',
        requestId: '{{ boomerangRequestId | raw }}',
    };
{% endif %}
