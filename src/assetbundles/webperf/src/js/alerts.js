import Vue from 'vue';
import VeeValidate from 'vee-validate';

Vue.use(VeeValidate);
// Create our vue instance
const vm = new Vue({
    el: "#cp-nav-content",
    components: {
    },
    data: {
    },
    mounted() {
    },
});
